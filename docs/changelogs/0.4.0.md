# 0.4.0 — 2026-03-27

> Pull requests come to the CLI, alongside more resilient field handling and
> improved secret management.

### Added

- **Pull request commands** — first set of PR CLI commands (`008-pull-request-handling`, #15).
- **`list fields`** — list available work item fields.

### Changed

- **More resilient field handling** — tolerant parsing/handling of work item fields (#16).
- **Improved secret handling** (#14).

### Fixed

- Resolved remaining SonarCloud findings, including reduced complexity in the `config` command.
