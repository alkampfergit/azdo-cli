# 0.7.0 — 2026-04-03

> Cleaner markdown field rendering in `get-item` output.

### Fixed

- **Markdown field label formatting** — correct label formatting for markdown fields in `get-item` output, for both single- and multi-line fields (`012-fix-markdown-field-formatting`, #23).

### Changed

- Config command flow improvements ("config automata").
